#include "../DynVector.h"

#include <iostream>

int main()
{
  DynVector<double> v(2);
  DynVector<double> w(5);
  for (int i = 0; i < 5; i++)
    w[i] = 1.1*i;


  v[0] = 4.4;
  v[1] = 3.3;
  
   for (int i = 0; i < 5; i++)
     std::cout << w[i] << std::endl;

   //   v.insert(7.7);

   std::cout << v.length << '\t' << w.length << std::endl;
  
  return 0;
}
